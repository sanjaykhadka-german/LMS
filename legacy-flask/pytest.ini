[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    smoke: minimal "does it load" checks
    sanity: one happy-path assertion per filter option
    regression: exhaustive coverage incl. edge cases
    backend: Flask test-client tests (no browser)
    e2e: Playwright tests against a live server
